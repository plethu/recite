/* Presentation only: callers own data, selection and workflow actions. */
function jobStatus(title,detail,action=''){return `<div class="job" role="status"><span class="spinner" aria-hidden="true"></span><div><strong>${title}</strong><small>${detail}</small></div>${action}</div>`;}
function comparisonView(beforeLabel,afterLabel,rows,context,{unified=false,unchanged=''}={}){return `<section class="comparison-view ${unified?'unified':''}" aria-label="Compare changes"><div class="comparison-toolbar"><span>${rows.length} changes · ${esc(context)}</span><button type="button" data-act="toggle-comparison" aria-pressed="${!!unified}">Unified view</button><button type="button" data-act="next-change">Next change ↓</button></div><div class="comparison-head"><strong>${beforeLabel}</strong><strong>${afterLabel}</strong></div>${rows.map((row,i)=>`<section class="change" tabindex="-1" data-change="${i}"><h3>${esc(row.label)}${row.kind?` · ${esc(row.kind)}`:''}</h3><div class="change-pair"><div><span class="change-label">${beforeLabel}</span>${diffText(row.before,row.after,false,row.kind==='Added'?'Not present in previous source':'No translation yet')}</div><div><span class="change-label">${afterLabel}</span>${diffText(row.after,row.before,true,row.kind==='Removed'?'Removed from current source':'No translation yet')}</div></div></section>`).join('')}${unchanged?`<details><summary>Unchanged context</summary><p class="prose">${esc(unchanged)}</p></details>`:''}</section>`;}
function diffText(text,other,added,empty){if(!text)return `<p class="empty-text">${empty}</p>`;const a=text.split(/(\s+)/),b=other.split(/(\s+)/);let start=0,end=0;while(start<a.length&&start<b.length&&a[start]===b[start])start++;while(end<a.length-start&&end<b.length-start&&a[a.length-end-1]===b[b.length-end-1])end++;const middle=a.slice(start,a.length-end).join('');return `<p class="diff-text">${esc(a.slice(0,start).join(''))}${middle?`<${added?'ins':'del'}>${esc(middle)}</${added?'ins':'del'}>`:''}${esc(end?a.slice(-end).join(''):'')}</p>`;}
